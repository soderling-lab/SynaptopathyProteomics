#!/bin/bash
#SBATCH --job-name=SLURM
#SBATCH -a 1       
#SBATCH --ntasks=1 --nodes=1
#SBATCH --mail-user=twesleyb10@gmail.com --mail-type=END  
#SBATCH -e ./out/job_%j.err
#SBATCH -o ./out/job_%j.out 
#SBATCH -c 16
#SBATCH --mem=16G      
#SBATCH -p scavenger

# Get filename and extension of input script.
filename=$1
extension=${filename##*\.} 

# Appropriately execute python or R script. 
if [ $extension == "py" ]
then
	echo "$filename"
elif [ $extension == "R" ]
then
	echo "$filename"
else
	echo "$extension files are not currently supported!"
fi
